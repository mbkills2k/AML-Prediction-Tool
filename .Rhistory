"Hello World"
3+4
# Assigning values to variables
x <- 5
y <- 10
# Performing operations with variables
z <- x + y
z  # This will display the value of z
# Creating a string variable
my_string <- "Hello, R!"
# Printing the string
print(my_string)
# Concatenating strings
greeting <- paste("Yellow", "World!")
print(greeting)
# Creating a string variable
my_string <- "Hello, R!"
# Printing the string
print(my_string)
# Concatenating strings
greeting <- paste("Yellow", "World!")
print(greeting)
# Creating a numeric vector
numbers <- c(1, 2, 3, 4, 5)
# Accessing elements in the vector
first_number <- numbers[1]
print(first_number)
# Basic operations on vectors
sum_numbers <- sum(numbers)
print(sum_numbers)
# Defining a function that adds two numbers
add_numbers <- function(a, b) {
result <- a + b
return(result)
}
# Using the function
sum_result <- add_numbers(3, 7)
print(sum_result)
# Defining a function that adds two numbers
add_numbers <- function(a, b) {
result <- a + b
return(result)
}
# Using the function
sum_result <- add_numbers(3, 7)
print(sum_result)
# Remove all variables in the workspace
rm(list = ls())
# Verify that everything is removed
print(ls())
#Remove all variables in the wokplaces
rm(list= 1s())
rm(list = 1s())
rm(list = ls())
x <- 10
y <- "Hello, World!"
my_list <- list(name = "Alice", age = 25, scores = c(90, 85, 88))
rm(list = 1s())
rm(list = ls())
rm(list =)
my_list <- list(name = "Alice", age = 25, scores = c(90, 85, 88))
my_list <- list(name = "Alice", age = 25, scores = c(90, 85, 88))
# Install Bioconductor and the required packages
if (!requireNamespace("BiocManager", quietly = TRUE))
install.packages("BiocManager")
# Install DiffBind and ChIPseeker
BiocManager::install("DiffBind")
BiocManager::install("ChIPseeker")
BiocManager::install("TxDb.Hsapiens.UCSC.hg19.knownGene")
BiocManager::install("clusterProfiler")
# Load the libraries
library(DiffBind)
library(ChIPseeker)
library(TxDb.Hsapiens.UCSC.hg19.knownGene)  # Human genome annotation
library(clusterProfiler)
# Define the sample sheet (CSV file listing your samples and conditions)
samples <- dba(sampleSheet="sample_sheet.csv")
library(DiffBind)
library(ChIPseeker)
library(TxDb.Hsapiens.UCSC.hg19.knownGene)  # For genome annotation
library(DiffBind)
library(ChIPseeker)
library(TxDb.Hsapiens.UCSC.hg19.knownGene)  # For genome annotation
setwd("path/to/your/project/folder")
